{
  "name": "twitch-chat-bot",
  "private": true,
  "workspaces": [
    "common",
    "server",
    "web"
  ],
  "scripts": {
    "dev:fullstack": "concurrently -n WEB,SERVER -c blue,gray \"yarn --cwd web rdev\" \"yarn --cwd server dev\""
  },
  "keywords": [],
  "author": "Phil Bladen",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
  }
}
