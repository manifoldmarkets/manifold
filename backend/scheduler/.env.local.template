# Local-only development environment for Scheduler
# Copy this to .env.local and fill in values from `npx supabase start`

# Enable local-only mode (skip Firebase, use local Supabase)
LOCAL_ONLY=true

# Supabase connection (from `npx supabase start` output)
SUPABASE_HOST=127.0.0.1
SUPABASE_PORT=54322
SUPABASE_PASSWORD=postgres

# These come from `npx supabase start` output
# anon key:
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# JWT secret (for signing tokens)
SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long

# API secret (can be any value for local dev)
API_SECRET=local-dev-secret

# Scheduler password (for basic auth on scheduler web UI)
SCHEDULER_AUTH_PASSWORD=local-scheduler-password

# Scheduler port (default 8080)
PORT=8081
