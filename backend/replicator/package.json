{
  "name": "replicator",
  "description": "GCR service for replicating Firestore writes to Supabase.",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "yarn compile && rm -rf dist && mkdir -p dist/backend/replicator && cp -R ../../common/lib dist/common && cp -R ../shared/lib dist/backend/shared && cp -R lib/src dist/backend/replicator/src && cp ../../yarn.lock dist && cp package.json dist",
    "regen-types": "supabase gen types typescript --project-id pxidrgkatumlvfqaxcll --schema public > ../common/supabase/schema.ts",
    "regen-types-dev": "supabase gen types typescript --project-id mfodonznyfxllcezufgr --schema public > ../common/supabase/schema.ts",
    "compile": "tsc -b",
    "watch": "tsc -w"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@google-cloud/pubsub": "3.2.1",
    "@supabase/supabase-js": "2.2.0",
    "express": "4.18.1",
    "firebase-admin": "11.2.0",
    "node-fetch": "2.6.7",
    "pg-promise": "11.0.2",
    "typescript": "4.9.3"
  },
  "devDependencies": {
    "@types/node-fetch": "2.6.2",
    "supabase": "^1.34.2"
  }
}
