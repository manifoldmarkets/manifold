export type ContractCardView = {
  name: 'view market'
  slug: string
  timestamp: number
  contractId: string
  creatorId: string
  groupSlugs: string[]
}

export type ContractView = {
  name: 'view market card'
  slug: string
  timestamp: number
  contractId: string
  creatorId: string
}
